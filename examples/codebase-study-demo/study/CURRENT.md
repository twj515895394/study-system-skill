# CURRENT：当前学习状态

> 新会话优先读这份文件。

## 当前课程

L01：系统总览与核心目录。

## 当前状态

已完成

## 最新 handoff

`handoffs/2026-06-25-L01-system-overview.md`

## 已确认理解

用户已经能够：

1. 区分 `cli/`、`core/`、`tools/`、`state/`、`config/` 的职责。
2. 解释 MiniAgentFlow 的主链路：CLI 输入 -> 参数解析 -> 任务构建 -> 调度执行 -> 状态保存。
3. 判断新功能通常应该先落在工具层还是调度层。

## 下一步

进入 L02《CLI 入口到任务调度的调用链》。建议使用 `study/_templates/lesson-types/code-trace.md.template`。

## 教学偏好提醒

继续使用 Mermaid 调用链图和表格。每次最多深入 3 个关键函数。

## 新会话续接方式

直接告诉用户：“我们正在学 MiniAgentFlow 源码，L01 系统总览已经完成。接下来进入 L02，从 CLI 入口追到任务调度，要继续吗？”

---
最后更新：2026-06-25
