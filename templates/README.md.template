# {{TOPIC}} 学习工作区

这是一个持续推进的学习工作区，用于系统学习「{{TOPIC}}」。

## 新会话从这里开始

按顺序读：

1. `STATE.json` —— 机器可读状态摘要，用来快速确认当前课次、状态和最新 handoff
2. `MASTER-PLAN.md` —— 唯一权威主计划
3. `MISSION.md` —— 为什么学这个，避免方向跑偏
4. `TEACHING-PROTOCOL.md` —— 教学风格、节奏与理解检查偏好
5. `CURRENT.md` —— 当前学习状态（人类可读版，直接告诉你接下来该做什么）
6. `PROGRESS.md` —— 整体进度看板
7. `handoffs/` 里最新一份 —— 上节课具体发生了什么

不要从头问“我们学到哪了”。读取上述文件后，直接基于 `STATE.json`、`CURRENT.md` 和最新 handoff 续接。

## 目录结构

```
study/
├── STATE.json               机器可读状态摘要
├── README.md                本文件
├── MASTER-PLAN.md            唯一权威主计划
├── MISSION.md                为什么学
├── TEACHING-PROTOCOL.md      教学协议与风格规范
├── CURRENT.md                当前状态，新会话优先读
├── PROGRESS.md               进度看板
├── COURSE-ROADMAP.md         阶段路线图
├── COURSE-LIST.md            课程清单
├── {{READING_MAP_NAME}}      资料阅读地图
├── QUESTION-PARKING-LOT.md   问题停车场，控制发散问题
├── REVIEW-SCHEDULE.md        复习计划，跟踪长期掌握
├── GLOSSARY.md               术语表（仅理解后写入）
├── NOTES.md                  用户偏好与临时笔记
├── handoffs/                 每节课交接文档
├── lessons/                  单课详细内容
├── reference/                复习资料（课程精华压缩版）
├── learning-records/         学习记录（仅理解检查通过后写入）
├── exercises/                练习产出
└── _templates/               工作区内置模板，方便跨 Agent 续接
```

## 状态取值

`STATE.json`、`CURRENT.md`、`PROGRESS.md`、`COURSE-LIST.md`、`handoffs/` 中统一使用以下状态：

未开始 / 进行中 / 待理解检查 / 待补课 / 补课中 / 已完成 / 需复习 / 暂停

## 每节课结束要更新什么

`STATE.json`、`PROGRESS.md`、`CURRENT.md`、写一份新 handoff、更新对应 `reference/`。

只有理解检查通过、状态为“已完成”时，才更新 `learning-records/`、`GLOSSARY.md`，并把对应知识点加入 `REVIEW-SCHEDULE.md`。

如果出现不阻塞当前主线的问题，先写入 `QUESTION-PARKING-LOT.md`，不要立刻打断课程。

## 状态校验

建议每次阶段收尾或新会话接手前运行：

```bash
python "${CLAUDE_SKILL_DIR}/scripts/validate_workspace.py" --root ./study
```

校验脚本只读不写，用于检查必需文件、状态枚举、`STATE.json`、`PROGRESS.md` / `COURSE-LIST.md` 一致性，以及 handoff 命名。

创建时间：{{DATE}}
